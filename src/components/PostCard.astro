---
import type { CollectionEntry } from "astro:content";
import TagBadge from "./TagBadge.astro";

const { post } = Astro.props;
const { title, date, tags, description } = post.data;
const base = (import.meta.env.BASE_URL ?? "/").replace(/\/$/, "") || "/";
const href = `${base}/posts/${post.slug}`;
---

<article class="bg-surface rounded-xl border border-border p-5 hover:shadow-md hover:border-text-muted/30 transition-all duration-200 group">
  <a href={href} class="block">
    <time class="text-xs text-text-muted mb-2 block">
      {date.toLocaleDateString("zh-CN", { year: "numeric", month: "long", day: "numeric" })}
    </time>
    <h2 class="text-lg font-semibold text-text group-hover:text-accent transition-colors leading-snug mb-2">
      {title}
    </h2>
    {
      description && (
        <p class="text-sm text-text-muted leading-relaxed line-clamp-2">
          {description}
        </p>
      )
    }
  </a>
  {
    tags.length > 0 && (
      <div class="flex flex-wrap gap-1.5 mt-3">
        {tags.map((tag) => (
          <a href={`${base}/tags/${tag}`}>
            <TagBadge tag={tag} />
          </a>
        ))}
      </div>
    )
  }
</article>
